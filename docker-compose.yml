services:
  app:
    image: python:3.10-slim
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8888:8888"
    command: >
      bash -lc "pip install -r requirements.txt &&
      python run_with_dashboard.py --port 8888"